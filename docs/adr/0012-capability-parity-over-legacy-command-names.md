# Prefer capability parity over legacy command names

The Go rewrite targets parity in what users can accomplish, not parity with every legacy command name. Remote installation is exposed through a simpler top-level `add` command aligned with `npx skills add`, while older source-specific command names are not carried forward when a clearer command surface exists.
